<?php 

    // ** -> PHP is dynamically typed, which means it automatically converts the variable to the correct data type based on its value.

    // String
    $name = "Juwel";

    // Integer
    $age = 25;

    // Float
    $price = 9.99;

    // Boolean
    $isStudent = true;

    // Array
    $colors = ["red", "green", "blue"];

    // Object
    class Car {
    public $brand = "Toyota";
    }
    $myCar = new Car();

    // NULL
    $unknown = null;


?>